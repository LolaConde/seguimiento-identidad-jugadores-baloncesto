Se realiza el reentrenamiento de un modelo de ReID. Esto se explica en el notebook `ReID_explicacion.ipynb`. 

Para consultar el código utilizado, se pueden consultar los notebooks `obtener_datos_reid.ipynb` para la obtención de datos para entrenar el modelo de reidentificación, y `entrenamiento_reid.ipynb` para el entrenamiento del modelo y la generación de gráficas.